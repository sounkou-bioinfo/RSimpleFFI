# Generate compile-time bitfield offset/size detection macros

Uses the Reddit r/cpp technique with \_\_builtin_ctzll/\_\_builtin_clzll
Works on GCC/Clang (which R always uses)

## Usage

``` r
generate_bitfield_detection_macros()
```
