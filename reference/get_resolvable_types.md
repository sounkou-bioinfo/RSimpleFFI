# Get list of resolvable type names

Returns a character vector of all type names that can be directly
resolved to FFI types, for use in can_resolve_typedef().

## Usage

``` r
get_resolvable_types()
```

## Value

Character vector of resolvable type names
