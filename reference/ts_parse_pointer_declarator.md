# Parse pointer declarator to extract pointer prefix and identifier

Parse pointer declarator to extract pointer prefix and identifier

## Usage

``` r
ts_parse_pointer_declarator(ptr_node, source_text)
```
