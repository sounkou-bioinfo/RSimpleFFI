# Generate wrapper code from parameter information

Generate wrapper code from parameter information

## Usage

``` r
generate_wrapper_code(
  func_name,
  return_type_ffi,
  param_names,
  param_types_c,
  param_types_ffi,
  is_variadic
)
```
