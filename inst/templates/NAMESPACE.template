# Generated by RSimpleFFI - edit with care

# Load native symbols from compiled code
useDynLib({{PACKAGE_NAME}}, .registration = TRUE)

import(RSimpleFFI)

# Export all generated functions
exportPattern("^r_")
exportPattern("^{{LIBRARY_NAME}}_")

# Export struct/union types and helpers
exportPattern("_t$")
exportPattern("^new_")
exportPattern("_to_list$")

# Export API mode struct functions (StructName_new, StructName_get, etc.)
exportPattern("^[A-Z][A-Za-z0-9_]*_(new|offsets|get|set)$")
